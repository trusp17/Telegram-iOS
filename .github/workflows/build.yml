name: SwiftGram Ultimate Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-13

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'recursive'
          fetch-depth: '0'

      - name: Set Xcode
        run: |
          XCODE_VERSION=$(cat versions.json | python3 -c 'import json,sys;obj=json.load(sys.stdin);print(obj["xcode"]);')
          sudo xcode-select -s /Applications/Xcode_$XCODE_VERSION.app/Contents/Developer

      - name: RUN ALL IN ONE (Move, Config, Build)
        env:
          API_ID: ${{ secrets.API_ID }}
          API_HASH: ${{ secrets.API_HASH }}
        run: |
          set -e # Падать при любой ошибке
          set -x # Показывать команды

          echo ">>> 1. MOVING SOURCE TO /Users/Shared <<<"
          sudo mkdir -p /Users/Shared
          cp -R $GITHUB_WORKSPACE /Users/Shared/telegram-ios
          cd /Users/Shared/telegram-ios

          echo ">>> 2. CREATING CONFIG (OVERWRITE) <<<"
          # Мы ВНУТРИ папки /Users/Shared/telegram-ios. Пишем конфиг ПРЯМО СЮДА.
          
          cat > build-system/appstore-configuration.json << EOF
          {
            "sg_config": {},
            "bundle_id": "org.trusp17.swiftgram",
            "api_id": "${API_ID}",
            "api_hash": "${API_HASH}",
            "team_id": "",
            "app_center_id": "0",
            "is_internal_build": "true",
            "is_appstore_build": "false",
            "appstore_id": "686449807",
            "app_specific_url_scheme": "sg",
            "premium_iap_product_id": "org.telegram.telegramPremium.monthly",
            "enable_siri": true,
            "enable_icloud": true
          }
          EOF

          echo ">>> 3. VERIFYING CONFIG <<<"
          # Читаем файл. Если тут нет sg_config, значит магия существует.
          cat build-system/appstore-configuration.json
          
          # Проверяем наличие строки sg_config грепом, чтобы упасть сразу, если её нет
          grep "sg_config" build-system/appstore-configuration.json || { echo "CRITICAL: sg_config MISSING!"; exit 1; }

          echo ">>> 4. PREPARING ENV <<<"
          BAZEL_USER_ROOT="/private/var/tmp/_bazel_containerhost"
          BUILD_NUMBER_OFFSET="$(cat build_number_offset)"
          APP_VERSION=$(cat versions.json | python3 -c 'import json,sys;obj=json.load(sys.stdin);print(obj["app"]);')
          COMMIT_COUNT=$(git rev-list --count HEAD)
          COMMIT_COUNT="$(($COMMIT_COUNT+$BUILD_NUMBER_OFFSET))"
          BUILD_NUMBER="$COMMIT_COUNT"
          
          echo "Build Number: $BUILD_NUMBER"

          echo ">>> 5. IMPORTING CERTS <<<"
          python3 build-system/Make/ImportCertificates.py --path build-system/fake-codesigning/certs

          echo ">>> 6. BUILDING IPA <<<"
          # ВАЖНО: Мы уже в нужной папке, путь к конфигу локальный
          python3 -u build-system/Make/Make.py \
            --bazelUserRoot="$BAZEL_USER_ROOT" \
            build \
            --configurationPath="build-system/appstore-configuration.json" \
            --codesigningInformationPath=build-system/fake-codesigning \
            --configuration=release_arm64 \
            --buildNumber="$BUILD_NUMBER" \
            --overrideXcodeVersion

          echo ">>> 7. COLLECTING ARTIFACTS <<<"
          OUTPUT_PATH="build/artifacts"
          mkdir -p "$OUTPUT_PATH"
          find bazel-out -name "Telegram.ipa" -exec cp {} $OUTPUT_PATH/SwiftGram.ipa \;
          ls -la $OUTPUT_DIR

      - name: Upload Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: SwiftGram-IPA
          path: /Users/Shared/telegram-ios/build/artifacts/SwiftGram.ipa
          retention-days: 7
